import request
import smtplib
import os


r=request.get ('https://onlinebookstore.com',timeout=5)
##trying to get request status from website and setting up the timeout for 5 sec.
def notify_user():
with smptplib.SMTP('GMAIL.COM',587) as smtp:
      smtp.ehlo()
      smtp.starttls()
      smtp.ehlo()
 smtp.login(EMAIL_ADDRESS,PASSWORD) ##Not necessary
 




try:
if r.status_code !=200:
# Not ok status
notify_user()
reboot_server()
else:
#Status is ok
except Exception as e:
 notify_user()
 reboot_server()

 
      
 
